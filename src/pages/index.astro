---
// Homepage — placeholder until the v5 homepage port lands.
// For now redirect visitors to the foundation preview so there's something
// real to see during the migration.
return Astro.redirect('/v5-foundation');
---
