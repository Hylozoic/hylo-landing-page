# Astro on Netlify: the @astrojs/netlify adapter (configured in
# astro.config.mjs) wires SSR routes to Netlify Functions; the rest of the
# site is prerendered HTML in dist/.

[build]
  command = "yarn build"
  publish = "dist"
